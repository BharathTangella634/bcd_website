# Local development overrides only.
# In production, sensitive values come from Google Secret Manager.
# Non-sensitive config is set in docker-compose.yml environment block.

MYSQL_HOST=10.87.192.4
MYSQL_PORT=3306
USE_CLOUD_SQL_CONNECTOR=true
CLOUD_SQL_IP_TYPE=PRIVATE
MYSQL_POOL_SIZE=25

# Secrets — set these locally or rely on Secret Manager in production:
# MYSQL_USER=
# MYSQL_PASSWORD=
# MYSQL_DB=
# CLOUD_SQL_CONNECTION_NAME=
